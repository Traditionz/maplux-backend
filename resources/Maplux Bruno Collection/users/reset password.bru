meta {
  name: reset password
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/auth/password/reset
  body: json
  auth: none
}

body:json {
  {
    "token": "{{resetToken}}",
    "password": "NewTest1234"
  }
}

settings {
  encodeUrl: true
}
