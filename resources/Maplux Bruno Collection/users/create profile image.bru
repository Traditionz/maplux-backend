meta {
  name: upsert profile image
  type: http
  seq: 11
}

put {
  url: {{baseUrl}}/auth/users/me/image
  body: json
  auth: inherit
}

body:json {
  {
    "imageExt": "png"
  }
}

settings {
  encodeUrl: true
}
